import { StyleSheet } from 'react-native';

export const savedStyles = StyleSheet.create({
    container: {
        flex: 1,
    },
    emptyStateContainer: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
}); 